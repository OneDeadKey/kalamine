#!/usr/bin/env python3
import sys
from kalamine import Layout, Template

for file in sys.argv[1:]:  # who needs argparse / docopt?
    layout = Layout(file)
    tpl = Template(layout)
    tpl.make_all('dist')
